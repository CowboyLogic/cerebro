# CLAUDE.md

> **`AGENTS.md` is the authoritative context for this repository.** Read it before taking
> any action. This file contains only Claude-specific addenda that do not belong in `AGENTS.md`.

---

## Claude-Specific Notes

**Running PowerShell commands:** Use `-NoProfile` to avoid terminal profile interference with
command execution.
